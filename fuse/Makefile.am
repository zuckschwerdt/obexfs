## Process this file with automake to produce Makefile.in

LDADD =				-lfuse -lpthread \
				-lopenobex -lbluetooth \
				-lobexftp -lcobexbfb -lbfb

obexfs_SOURCES =		obexfs.c

bin_PROGRAMS =			obexfs
